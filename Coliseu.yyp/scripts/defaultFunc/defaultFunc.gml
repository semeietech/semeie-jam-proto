playerHasHp();
getKeys();

// -- UPDATE INPUT
inputLeft = keyboard_check(leftKey);
inputRight = keyboard_check(rightKey);
inputDown = keyboard_check(downKey);
inputUp = keyboard_check(upKey);

inputPrimaryAction = keyboard_check_pressed(primaryActionKey);
inputSecundaryAction = keyboard_check_pressed(secundaryActionKey);
inputPrimarySuport = keyboard_check_pressed(primarySuportKey);
inputSecundarySuport = keyboard_check_pressed(secundarySuportKey);
